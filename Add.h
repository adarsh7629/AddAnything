#include<iostream>
#include <stdio.h>
#include <string>
using namespace std;

class Add{

    public:
    int add(int, int);
    string add(string, string);
    string add(int, string);
    string add(string, int);
};